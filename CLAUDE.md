Vamos a desarrollar una app usando flask/python que incorpore lo siguiente:

supabase como base de datos
desplagada en vercel
bootstrap para los estilos, define un archivo variables.css en /static para colocar colores personalizados a mi app

las funcionalidades deben las necesarias para manejar el inventario de una tienda pequeña, pero funcional

!!! importante: en el archivo SUPABASE.sql vamos air creando el esquema y/o modificaciones necesario de principio a fin del ciclo de desarrollo de tal modo que una ves finalizado el proyecto pueda usar ese archivo para replicar la base de datos en otros escenarios